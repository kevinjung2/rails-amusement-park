<% @attractions.each do |attraction| %>
  <% if @user.admin %>
    <%= link_to "Show #{attraction.name}", attraction %>
  <% else %>
    <%= link_to "Go on #{attraction.name}", attraction %>
  <% end %>
<% end %>

<%= link_to "New Attraction", '/attractions/new' if @user.admin%>
